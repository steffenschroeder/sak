# sak is a Swiss Army knife for my workflow

### Usage
`sak component` hides component
`sak .component` unhides component
`sak release component``releases component
`sak clone component` clones component
